import { API, apiJson } from './client';

/**
 * Вкладка «Кубок» (только admin): текущий обладатель кубка — победитель
 * ПРОШЛОГО месяца — и рейтинг ТЕКУЩЕГО месяца.
 *
 * Всё считает сервер (app/champion.py). Фронт не знает правил подсчёта —
 * иначе страница, значок в шапке и вкладка «Пользователи» однажды
 * разошлись бы в том, кто чемпион.
 */
export function fetchChampionBoard() {
  return apiJson(`${API}/champion`);
}
