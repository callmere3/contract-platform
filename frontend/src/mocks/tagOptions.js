// Значения дословно из backend/app/tags.py — в реальной версии придут
// через GET /tags (единственный источник правды, см. routers_tags.py).
// Обратите внимание: "АВАНС_ОБЯЗАТЕЛЬСТВО" — с подчёркиванием, не с пробелом.
export const TAG_OPTIONS = {
  countries: ['РУ', 'КЗ'],
  contragentTypes: ['СГ', 'ИП', 'ООО'],
  contractFamilies: ['РОЯЛТИ', 'АВАНС', 'АВАНС_ОБЯЗАТЕЛЬСТВО'],
};
